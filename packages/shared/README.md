# Shared package

Framework-independent code shared by the mobile app and server functions:

- `types` — domain TypeScript types
- `schemas` — boundary validation schemas
- `domain` — pure deterministic domain logic

Do not add React Native, Supabase client, or provider-specific code here.
